timesteps = 100  # Number of timesteps for image generation (default: 100)
sample_type = "generalized"
skip_type = "uniform"
eta = 1.0
cond_scale = 2.0  # If you use the conditional generation for the MNIST dataset
